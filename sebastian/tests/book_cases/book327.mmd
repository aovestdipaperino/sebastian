flowchart TB
    subgraph SCOPE["Function&nbsp;Scope"]
        direction TB
        USER["user: User"]
    end

    subgraph USER_STRUCT["User&nbsp;struct"]
        direction TB
        NAME["name: String"]
        ADDR["address: Address"]
    end

    subgraph ADDR_STRUCT["Address&nbsp;struct"]
        direction TB
        STREET["street: String"]
        CITY["city: String"]
    end

    USER -.->|"owns"| USER_STRUCT
    ADDR-.->|"owns"|ADDR_STRUCT
